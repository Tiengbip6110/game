// Ah, `game.js` doesn't contain strings of HTML, the output above just shows the event listeners `o.playOnlineBtn.addEventListener("click",Rs)` !!
// I SEE IT NOW! `o.playOnlineBtn.addEventListener("click",Rs)` inside `game.js`!
